# Implementation Plan: Readme update

Source spec: [spec.md](./spec.md)

Replace `README.md` with a student-facing Todoish guide, set layout metadata to Todoish, and add short role comments on six canonical files.

## Technical context

- Next.js 16 / React 19 / Prisma 7 (SQLite) / Storybook 10 / Tailwind 4 / Vitest / Playwright
- App is a single-user todo list named **Todoish** at `/` only
- `package.json` `name` is already `todoish` — do not change it
- Header/Footer already say Todoish — do not change them
- `src/app/layout.tsx` metadata is still Create Next App: `title: "Create Next App"`, `description: "Generated by create next app"`
- Current `README.md` is scratch notes plus the default `create-next-app` README — replace entirely; do not preserve that content
- Lecture source (do not move, edit, link, path, or name in the README): `specs/008-readme/docs.md/tooling_lecture.md`
- `.env.example` already exists (`DATABASE_URL="file:./prisma/dev.db"`)
- `prisma/dev.db` and `.env` are gitignored
- Scripts already exist: `dev`, `storybook` (`-p 6006`), `test`, `test:e2e`, `prisma:generate`, `prisma:migrate`
- `vitest.config.ts` already has a Storybook docs URL comment — keep it
- `createTodo.ts` starts with `"use server"` — that directive stays the first statement
- Agent conventions stay in `AGENTS.md` — do not rewrite it

## Target structure

```
README.md                                              (replace entirely)
src/app/layout.tsx                                     (metadata only)
src/app/page.tsx                                       (file-role comment)
src/components/atoms/Button/Button.tsx                 (file-role comment)
src/actions/todo/createTodo/createTodo.ts              (file-role comment)
src/prisma/prismaClient.ts                             (file-role comment)
.storybook/main.ts                                     (file-role comment)
vitest.config.ts                                       (file-role comment; keep existing URL comment)
```

Do not add, move, or edit `specs/008-readme/docs.md/tooling_lecture.md`.

## README.md

Replace the whole file. Audience: students cloning this repo. Concise. No SDD/spec-authoring tutorial, no how-to-add-a-component, no copy of `AGENTS.md`, no “Learn More”, no “Deploy on Vercel”, no CI/CD.

### Heading and opening

H1: `# Todoish — course sample`

Opening paragraph: this is **Todoish**, a single-user todo sample for the course, not a production app.

Then a sentence pointing at `AGENTS.md` for coding conventions (do not paste those conventions).

### Stack

Heading: `## Stack`

Markdown bullets (not a table), in this order. Link the tool name to the lecture URL. One-line “what it is here” plus a file pointer. Do not mention `tooling_lecture.md`.

| Tool | Link | One-line role | File pointer |
| --- | --- | --- | --- |
| React 19 | https://react.dev/ | UI library | `src/components/` |
| Next.js | https://nextjs.org/ | App Router | `src/app/page.tsx`, `src/app/layout.tsx` |
| Prisma | https://www.prisma.io/ | SQLite ORM | `prisma/schema.prisma`, `src/prisma/prismaClient.ts` |
| Storybook | https://storybook.js.org/ | UI in isolation | `.storybook/`, colocated `*.stories.tsx` |
| Atomic Design | https://atomicdesign.bradfrost.com/chapter-2/ | UI folder map | `src/components/{atoms,molecules,organisms,templates,pages}` |
| Tailwind CSS | https://tailwindcss.com/ | utility classes | `src/app/globals.css` |
| Vite | https://vite.dev/ | Storybook and Vitest toolchain, not the Next.js bundler | `.storybook/main.ts`, `vitest.config.ts` |
| Vitest | https://vitest.dev/ | component/story/action tests | colocated `*.test.tsx`, `tests/actions/` |
| Playwright | https://playwright.dev/ | e2e | `tests/e2e/`, `playwright.config.ts` |
| SQLite | none (not on the lecture stack slide) | local DB used by Prisma; `prisma/dev.db` is gitignored and created on migrate | `prisma/schema.prisma` |

### Setup (first run)

Heading: `## Setup`

Numbered, this order only:

1. Install Node.js and npm. Clone this repo.
2. `npm install`
3. Copy `.env.example` to `.env` (do not commit `.env`)
4. `npm run prisma:generate`
5. `npm run prisma:migrate` (applies existing migrations; creates `prisma/dev.db`)
6. `npm run dev` → http://localhost:3000/

Do not list `npx playwright install` as a required first-run step.

### Scripts

Heading: `## Scripts`

After setup, not as first-run steps. Name + one line each:

- `dev` — Next.js app at http://localhost:3000/
- `storybook` — Storybook UI on port 6006
- `test` — Vitest (storybook, components, and actions projects)
- `test:e2e` — Playwright e2e
- `prisma:generate` — generate the Prisma client
- `prisma:migrate` — apply existing migrations; creates `prisma/dev.db`

### File structure

Heading: `## File structure`

ASCII tree with a one-line note on each entry. Include the spec’s required paths **plus** the extra files already named in Stack (`globals.css`, `page.tsx`, `layout.tsx`, `vitest.config.ts`, `playwright.config.ts`):

```
.
├── src/
│   ├── app/                         # routes (`page.tsx` is `/`)
│   │   ├── page.tsx                 # `/` — loads todos, injects Server Actions
│   │   ├── layout.tsx               # root layout and document metadata
│   │   └── globals.css              # Tailwind entry
│   ├── components/                  # atomic UI
│   │   └── atoms/Button/Button.tsx  # canonical atom
│   ├── actions/                     # Server Actions
│   │   └── todo/createTodo/createTodo.ts
│   ├── prisma/prismaClient.ts       # Prisma client used by actions
│   └── generated/prisma/            # generated client; do not edit
├── prisma/                          # schema and migrations
├── .storybook/                      # Storybook config
├── tests/
│   ├── actions/                     # action tests (`prisma/test.db`)
│   └── e2e/                         # Playwright (`prisma/dev.db`)
├── specs/                           # course specs
├── vitest.config.ts                 # Vitest projects (storybook, components, actions)
├── playwright.config.ts             # Playwright e2e config
└── AGENTS.md                        # conventions for this repo
```

After the tree, mention: action tests must not use `prisma/dev.db`; e2e uses `prisma/dev.db` and unique titles. Do not tell students to wipe the DB.

## Student-visible labels

**File:** `src/app/layout.tsx`

Change metadata only:

```ts
export const metadata: Metadata = {
  title: "Todoish",
  description:
    "A single-user todo app used as sample code for a frontend course.",
};
```

Do not change Header/Footer copy. Do not change `package.json` `name`.

## Code comments

Short `/* */` blocks only. Name the tool and the role of the file. Do not tutorialize the whole file. Do not add comments to `src/generated/prisma/`.

Place each block at the top of the file, except `createTodo.ts`: `"use server"` stays first; the comment goes immediately after it.

**`src/app/page.tsx`**

```ts
/*
 * Next.js App Router route for `/`.
 * Loads todos and injects Server Actions into the page component.
 */
```

**`src/components/atoms/Button/Button.tsx`**

```ts
/*
 * Atomic Design atom. Tailwind variants are defined in this file.
 */
```

**`src/actions/todo/createTodo/createTodo.ts`**

```ts
"use server";

/*
 * Server Action. Prisma write for a new todo.
 */
```

**`src/prisma/prismaClient.ts`**

```ts
/*
 * Prisma client for server code only. Do not import from UI.
 */
```

**`.storybook/main.ts`**

```ts
/*
 * Storybook config. Uses Vite (`@storybook/nextjs-vite`), not the Next.js bundler.
 */
```

**`vitest.config.ts`**

Add this block above the existing Storybook URL comment. Do not remove that existing comment.

```ts
/*
 * Vitest projects: storybook, components, and actions.
 */
```

## Implementation order

1. Replace `README.md` with the sections above.
2. Update `src/app/layout.tsx` metadata.
3. Add the six file-role comments (keep `"use server"` first in `createTodo.ts`; keep the existing Vitest Storybook URL comment).
4. Confirm document title/description in the running app. Do not change Header/Footer or existing tests.

## Out of scope

- Editing or moving `specs/008-readme/docs.md/tooling_lecture.md`
- Rewriting `AGENTS.md`, adding README rules there, or documenting the spec/skills workflow
- Component/convention how-tos (those stay in `AGENTS.md`)
- New npm packages, new routes, auth, extra product pages
- CI/CD, Vercel, Chromatic
- `npx playwright install` as a required first-run step
- Changing Header/Footer copy or existing Todoish tests
- Renaming `package.json` `name`
- Editing `.env.example`
